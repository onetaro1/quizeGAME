'use strict';
{
  const question = document.getElememtById('question');
  const choices = document.getElememtById('choices');
  const btn = document.getElememtById('btn');

  const quizSet = [
    {q:'what is A?',c:['A01','A02','A03']},
    {q:'what is B?',c:['B01','B02','B03']},
    {q:'what is C?',c:['C01','C02','C03']},
  ];

  let currentNum = 0;
}
